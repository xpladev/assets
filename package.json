{
  "name": "xpla-assets",
  "version": "1.0.0",
  "description": "",
  "hompage": "",
  "author": "",
  "repository": "",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node index.js",
    "sync": "aws s3 sync . s3://assets.xpla.io --exclude '.git/*' --exclude '.vscode/*' --exclude 'node_modules/*' --exclude '.eslintignore' --exclude '.eslintrc' --exclude '.gitignore' --exclude '.prettierignore' --exclude '.prettierrc' --exclude 'package*' --exclude '.DS_Store' --exclude '*/.DS_Store' --exclude '*.js' --exclude 'README.md' --cache-control 'no-cache'",
    "update": "npm run build && npm run sync"
  },
  "devDependencies": {
    "@babel/core": "^7.18.9",
    "@babel/eslint-parser": "^7.18.9",
    "eslint": "^8.20.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "glob": "^8.0.3"
  }
}
